#include<bits/stdc++.h>

using namespace std;

int main() {
    vector<int> arr(2);
    arr.push_back(5);
    for (int i = 0; i < arr.size(); i++) {
        cout << arr.at(i) << endl;
    }
    
    return 0;
} 